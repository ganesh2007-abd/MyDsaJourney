/*

Approach

1)we first store the directions like dx and dy so when ever we wan tot change the pos ..we do pos++ and add dx[pos],dy[pos] to row and col numbers
2)we stop printing the numbers when the no of pronter numbers become n2
3)at each iterationn we first check tht nextrow,nextcol is valid or not then only go to next row and next col

*/